# Web Портфолио с системой комментариев

Веб-приложение на Flask с поддержкой авторизации через социальные сети и системой комментариев.

## Особенности

- ✨ Авторизация через Яндекс с получением аватара и имени
- 💬 Система комментариев с возможностью удаления собственных комментариев
- 📱 Адаптивный дизайн
- 🎨 Темный интерфейс с анимациями
- 📊 SQLite база данных для хранения пользователей и комментариев

## Установка

### Требования
- Python 3.8+
- pip

### Шаги установки

1. **Клонирование репозитория**
```bash
git clone <your-repo-url>
cd web
```

2. **Создание виртуального окружения**
```bash
python3 -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

3. **Установка зависимостей**
```bash
pip install -r requirements.txt
```

4. **Конфигурация переменных окружения**
Создайте файл `.env` в корневой директории проекта:
```
SECRET_KEY=your-secret-key-here

# Яндекс OAuth
YANDEX_CLIENT_ID=your-client-id
YANDEX_CLIENT_SECRET=your-client-secret
YANDEX_REDIRECT_URI=http://localhost:5000/auth/yandex/callback
```

## Получение Яндекс OAuth Credentials

1. Перейдите на [Яндекс OAuth Console](https://oauth.yandex.ru/client/my)
2. Нажмите "Зарегистрировать новое приложение"
3. Заполните следующие данные:
   - **Название приложения**: Ваше имя приложения
   - **Ссылка на приложение**: http://localhost:5000 (для разработки)
   - **Redirect URI**: http://localhost:5000/auth/yandex/callback
   - **Требуемые права**:
     - login:email (доступ к email)
     - login:avatar (доступ к аватару)
     - login:info (доступ к информации профиля)

4. После создания приложения скопируйте:
   - **Client ID** → в переменную `YANDEX_CLIENT_ID`
   - **Client Secret** → в переменную `YANDEX_CLIENT_SECRET`

## Запуск приложения

```bash
source venv/bin/activate  # Активация виртуального окружения
python3 run.py
```

Приложение будет доступно по адресу: http://localhost:5000

## Структура проекта

```
web/
├── src/
│   ├── __init__.py
│   ├── web.py          # Основное Flask приложение
│   └── dp.py           # Модели БД (User, Comment)
├── templates/
│   ├── base.html       # Базовый шаблон
│   ├── index.html      # Главная страница
│   ├── comments.html   # Страница комментариев
│   ├── social_login.html
│   └── ...
├── static/
│   ├── css/
│   │   └── styles.css
│   ├── images/
│   └── ...
├── .env                # Переменные окружения (в .gitignore)
├── requirements.txt    # Зависимости Python
└── run.py             # Точка входа приложения
```

## Основной функционал

### Авторизация
- Пользователи могут входить через Яндекс
- При входе автоматически загружается аватар и имя из профиля Яндекса

### Комментарии
- Авторизованные пользователи могут добавлять комментарии
- На странице комментариев отображаются:
  - Аватар пользователя (из профиля Яндекса)
  - Имя пользователя
  - Текст комментария
  - Дата и время комментария
- Пользователи могут удалять только свои комментарии

### Главная страница
- Профильная карточка с фото
- Раздел "Обо мне"
- Информация о проектах и навыках

## API Endpoints

- `GET /` - Главная страница
- `GET /comments` - Страница с комментариями
- `GET /social_login` - Страница авторизации
- `GET /auth/yandex` - Инициация авторизации через Яндекс
- `GET /auth/yandex/callback` - Callback от Яндекса
- `POST /add_comment` - Добавление комментария
- `POST /delete_comment/<id>` - Удаление комментария
- `GET /logout` - Выход из системы
- `GET /api/comments/<page>` - API для получения комментариев

## Переменные сессии

После авторизации в сессии сохраняются:
- `user_id` - ID пользователя
- `user_nickname` - Имя пользователя
- `user_avatar` - URL аватара
- `user_email` - Email пользователя

## Безопасность

- **SECRET_KEY** должен быть изменен в production окружении
- **.env файл** должен быть добавлен в `.gitignore` для защиты конфиденциальных данных
- **SSL/HTTPS** обязателен в production окружении
- Используется CSRF защита через Flask sessions

## Лицензия

MIT License

## Автор

Филичкин Богдан
